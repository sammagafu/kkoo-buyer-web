import type { RouteLocationRaw } from 'vue-router'

/** Canonical buyer home / dashboard — WebMarket in home mode. */
export const BUYER_DASHBOARD_PATH = '/marketplace'

export const BUYER_DASHBOARD_ROUTE: RouteLocationRaw = {
  name: 'buyer.marketplace',
}
